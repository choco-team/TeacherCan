빈 폴더 커밋하기 위한 파일, 삭제 요망

여러 페이지에서 공통으로 사용하게 될 상수 값을 넣어둔다.
constants 이름은 모두 대문자로 작성하며, 단어 사이를 언더스코어 _ 로 구분한다.


constants/ga.ts

export type DataType = {
  [IDOL.IDOL_OPEN]: {
    name: string;
  };
  [IDOL.IDOL_CATEGORY_CLICK]: {
    category: string;
  };
  [IDOL.IDOL_YEAR_CLICK]: {
    year: string;
  };
  [CAMERA.CAMERA_YEAR_CLICK]: {
    year: string;
  };

  [key: string]: unknown;
};

constants/case.ts

export const caseType = { general: '일반', wep: '개인회생' };