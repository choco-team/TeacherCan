import * as DialogPrimitive from '@radix-ui/react-dialog';

const Dialog = DialogPrimitive.Root;

const { DialogContent } = DialogPrimitive;
const { DialogTitle } = DialogPrimitive;
const { DialogDescription } = DialogPrimitive;
const { DialogTrigger } = DialogPrimitive;
const { DialogOverlay } = DialogPrimitive;

export {
  Dialog,
  DialogContent,
  DialogTitle,
  DialogDescription,
  DialogTrigger,
  DialogOverlay,
};
