import VolumeBlocksContainer from '@/containers/skip-number/volume-blocks/volume-blocks-container';

export const metadata = {
  title: '직육면체 부피 구하기',
};

export default function VolumeBlocksPage() {
  return <VolumeBlocksContainer />;
}
