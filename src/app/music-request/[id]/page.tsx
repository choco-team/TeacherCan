import MusicRequestRoomContainer from '@/containers/music-request-room/music-request-room-container';

export const metadata = {
  title: 'Music Request',
};

function MusicRequestRoom() {
  return <MusicRequestRoomContainer />;
}

export default MusicRequestRoom;
