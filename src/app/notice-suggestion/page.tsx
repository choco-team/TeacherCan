import NoticeSuggestionContainer from '@/containers/notice-suggestion/notice-suggestion-container';

export const metadata = {
  title: '알림장 문구 추천',
};

export default function NoticeSuggestion() {
  return <NoticeSuggestionContainer />;
}
