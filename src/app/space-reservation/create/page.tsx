import SpaceReservationCreateContainer from '@/containers/space-reservation/space-reservation-create-container';

export const metadata = {
  title: '공간 만들기',
};

export default function SpaceReservationCreatePage() {
  return <SpaceReservationCreateContainer />;
}
