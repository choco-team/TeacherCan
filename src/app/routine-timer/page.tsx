import RoutineTimerContainer from '@/containers/routine-timer/routine-timer-container';

export const metadata = {
  title: '루틴타이머',
};

function RoutineTimer() {
  return <RoutineTimerContainer />;
}

export default RoutineTimer;
